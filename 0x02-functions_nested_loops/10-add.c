#include "main.h"
/**
 * add- a function that adds two integers and returns the result
 * @i: intergers to add together
 * @j: intergers to add together
 * Return: the result
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
