#include "main.h"

/**
 * print_triangle - prints a triangle on the terminal
 * @size: of the triangle
 * good
 */
void print_triangle(int size)
{
	int i, j;

	if (size <= 0)
	{
		_putchar('\n');
		return;
	}
